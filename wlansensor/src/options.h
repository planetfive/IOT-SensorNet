#ifndef OPTIONS_H
#define OPTIONS_H

#include <Arduino.h>

extern String getSensorOptions(String sensortyp);
extern String getModulMode(String mode);
extern String getOutputMode(uint8_t mode);

#endif
